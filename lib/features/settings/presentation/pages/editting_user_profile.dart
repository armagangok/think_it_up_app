import 'package:flutter/material.dart';

class EdittingUserProfileScreen extends StatelessWidget {
  const EdittingUserProfileScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}