import 'package:flutter/material.dart';

class EdittingUserProfilePage extends StatelessWidget {
  const EdittingUserProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
